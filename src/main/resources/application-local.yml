server:
  port: 9061

spring:
  datasource:
    url: "jdbc:h2:mem:testdb;INIT=CREATE SCHEMA IF NOT EXISTS testdb \\;SET SCHEMA testdb"
    username: testApi
    password: rootApi
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: true
  sql:
    init:
      mode: always
      continue-on-error: true
      encoding: UTF-8
      #schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
  h2:
    console:
      enabled: true
  security:
    user:
      name: desarrollo
      password: microlab
app:
  jwt:
    secret-key: 3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b
